package vn.edu.iuh.fit.dto;import lombok.Data;import vn.edu.iuh.fit.entity.Image;import vn.edu.iuh.fit.entity.TourGuide;import vn.edu.iuh.fit.enums.TourType;import java.math.BigDecimal;import java.util.List;import java.util.Objects;@Datapublic class TourDetailDTO {    private long tourId;    private String tourName;    private String tourDescription;    private String startLocation;    private Integer duration;    private TourType tourType;    private List<DepartureDTO> departures;    private List<DestinationDTO> destinations;    private List<ReviewDTO> reviews;    private List<ImageDTO> images;//    private TourGuide tourGuide;    @Override    public boolean equals(Object o) {        if (this == o) return true;        if (!(o instanceof TourDetailDTO that)) return false;        return getTourId() == that.getTourId();    }    @Override    public int hashCode() {        return Objects.hash(getTourId());    }}