package vn.edu.iuh.fit.service;import vn.edu.iuh.fit.dto.UserDTO;import vn.edu.iuh.fit.entity.Account;import vn.edu.iuh.fit.entity.Booking;import vn.edu.iuh.fit.entity.User;import java.util.Optional;import java.util.UUID;public interface AccountService extends CrudService<Account, Long> {    //Đăng kí    User register(String username, String password, User user);    //Đăng nhập    UserDTO login(String username, String password);    //Xóa tài khoản    void deleteAccount(long accountId);    // Đổi mật khẩu    void changePassword(long accountId, String newPassword);    // Đặt lại mật khẩu    void resetPassword(String username, String newPassword);    // Kiểm tra tài khoản tồn tại    boolean existsByUsername(String username);    //Lấy email bởi tên tài khoản    String getEmailByUsername(String username);}