package vn.edu.iuh.fit.service;import vn.edu.iuh.fit.entity.Account;import vn.edu.iuh.fit.entity.Booking;import vn.edu.iuh.fit.entity.User;import java.util.Optional;import java.util.UUID;public interface AccountService extends CrudService<Account, UUID> {    //Đăng kí    UUID register(String username, String password, User user);    //Đăng nhập    Account login(String username, String password);    //Xóa tài khoản    void deleteAccount(UUID accountId);    // Đổi mật khẩu    void changePassword(UUID accountId, String newPassword);    // Đặt lại mật khẩu    void resetPassword(String username, String newPassword);}